import React from "react";

const Header: React.FC = () => {
    return (
        <header style={{ height: "60px", background: "#34495e", color: "#fff", padding: "10px 20px", display: "flex", alignItems: "center" }}>
            <h2>Painel de Controle</h2>
        </header>
    );
};

export default Header;
